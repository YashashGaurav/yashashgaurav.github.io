---
name: serve-test
description: Start local development server and run tests
---

Start local development server and run tests:

1. Run `hugo server -D` to start the dev server
2. If Playwright is installed, run `npx playwright test` for E2E validation
3. Report any build errors or test failures
